import { useContext } from "react";

import { CVContext } from "../CVContext";

const Form = () => {
  const { cvInfo, setCVInfo } = useContext(CVContext);

  /* 
    Basically a lot of fields
    - If use use the components, we can simplify things
  */
  return <div>


  </div>;
};

export default Form;
